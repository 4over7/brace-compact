---
name: brace-compact
description: Save session state before ending a conversation — whether for /compact, exit, restart, or end of day. Use when the user says "brace compact", "prepare for compact", "save progress", or before any session exit.
disable-model-invocation: false
allowed-tools: Read, Write, Edit, Bash, Glob, Grep
argument-hint: "[optional session notes]"
---

# Prepare for Context Compaction

Persist all important session knowledge before `/compact` truncates the conversation. After compact, `/resume` restores the saved state.

## Steps

Execute ALL steps in order, then output the summary.

### 1. Gather current state

Run these in parallel:

```bash
git status --short
```

```bash
git log --oneline -5
```

Check for any background tasks or temporary processes mentioned in the conversation.

### 2. Update project memory

Find the project's memory index (`MEMORY.md` in the memory directory). Review the current session and update if ANY of these changed:

- **Features / architecture** — code additions, refactors, new modules
- **Decisions + reasoning** — "chose X over Y because Z" (especially rejected approaches)
- **Debugging conclusions** — "X fails because Y, workaround is Z" (NOT in code, WILL be lost)
- **Environment changes** — servers started, config changed, ports opened, accounts created
- **Pending work** — what's started but unfinished, with enough detail to resume cold
- **Bugs discovered** — known issues not yet fixed

Rule: if you learned something non-obvious during this session that isn't captured in code or git history, it MUST go into memory.

### 3. Write resume checkpoint

Write `.claude/resume.md` (overwrite if it exists).

**Important — avoid the Write-tool guard**: if `.claude/resume.md` already
exists (it usually does, left over from the previous session's compact), the
Write tool will refuse to overwrite it until this session has first `Read` it.
So before calling Write:

1. Check existence via Bash `ls .claude/resume.md 2>/dev/null` or Glob.
2. If it exists, call `Read` on it once (even just the first few lines).
3. Then call `Write` with the new content.

If the file doesn't exist (e.g. first-ever compact in this repo), skip the
Read step — Write will create it. The `.claude/` directory is guaranteed to
exist per the system prompt; no `mkdir` needed.

Use this exact structure for the content:

```markdown
# Resume Checkpoint

> Generated by /brace-compact at {YYYY-MM-DD HH:MM}

## Task in progress
{What the user was working on — 1-2 sentences}

## Current state
{What's done, what's partially done, what hasn't started}

## Critical context
{Things the AI MUST know to continue. Include:
- Non-obvious technical facts discovered during debugging
- Workarounds currently in place
- Config quirks (e.g. "use model X not Y because Z returns error")
- Architecture decisions made and WHY
- Server/process state that isn't in code}

## Next steps
1. {First concrete action to take}
2. {Second action}
3. {etc.}

## Files touched this session
{List of key files modified, created, or deleted — helps orient the AI}

## Active environment
{Running processes, open ports, SSH sessions, temporary files — anything that exists outside of git}
```

If `$ARGUMENTS` is provided, add a `## Session notes` section with the content.

### 4. Check plans

If an active plan file exists in `.claude/plans/`, verify it reflects current progress. Mark completed steps.

### 5. Save feedback

If the user gave corrections during this session ("don't do X", "always do Y"), save each as a feedback memory file.

### 6. Verify

Read back `.claude/resume.md` and confirm it contains enough context for a cold start. Ask yourself: "If I read ONLY this file and MEMORY.md, could I continue the user's work without asking them to re-explain?" If not, add what's missing.

### 7. Output summary

```
🔒 Compact preparation complete

📝 Memory: [updated / no changes needed]
📋 Plan: [updated / none active]
💾 Uncommitted: [none / N files — consider committing]
⚙️ Background: [none / list running tasks]
📌 Checkpoint: .claude/resume.md written
🔄 Session highlights:
  - [bullet points]

▶️ After /compact, say "resume" or run /resume to restore context.
Ready for /compact.
```
